const Test = require('../models/test1');
const Request = require('../models/request')
const {MongoClient} = require('mongodb')

const bookSlot = async (req,res)=>{

}

const slotDet = async (req,res)=>{
    const {email} = req.body;
}